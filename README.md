# AI-Image-classification
# AI Image Classification System  AI Image Classification System is a Machine Learning project that uses a Convolutional Neural Network (CNN) to classify images into categories. It currently supports Cats vs Dogs classification. The system processes an input image and predicts its class with a confidence score.
